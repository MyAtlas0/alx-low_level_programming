#include <stdio.h>

/* Main - using void as the main function */
int main(void)
{

/* Printf - funtion used to print input instead of puts*/
	printf( "with proper grammar, but the outcome is a piece of art,\n" );
	return 0;
/* Return: Used to return to 0 and terminate program*/

}
